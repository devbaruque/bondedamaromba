import { COLORS } from './colors';
import typography from './typography';
import spacing from './spacing';

export { COLORS } from './colors';
export const { FONT_FAMILY, FONT_SIZE, FONT_WEIGHT, TEXT_VARIANT, createTextStyle } = typography;
export const { SPACING, BORDER_RADIUS, SHADOW } = spacing;

export default {
  COLORS,
  FONT_FAMILY,
  FONT_SIZE,
  FONT_WEIGHT,
  TEXT_VARIANT,
  SPACING,
  BORDER_RADIUS,
  SHADOW,
  createTextStyle,
}; 